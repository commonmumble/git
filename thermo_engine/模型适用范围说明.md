# 传统相平衡模型适用范围说明

> **文档用途**：供负责模型适用范围统筹的同学（原夏鑫华角色）查阅，了解当前已整合的六个传统模型的详细信息，便于编写模型选择逻辑和适用范围文档。
>
> **文档版本**：v0.2.0（适配 thermo_engine）
>
> **对应代码**：`D:\Codex\thermo_engine\backends\`

---

## 模型概览

| 模型 | 文件 | 类型 | 状态 |
|---|---|---|---|
| Ideal/Raoult | `backends/ideal_raoult.py` | 理想定律 | ✅ **RUNNABLE** |
| Peng-Robinson | `backends/peng_robinson.py` | 状态方程 | ✅ **RUNNABLE** |
| NRTL | `backends/contracts/nrtl.py` | 活度系数 | 📝 **CONTRACT** |
| UNIQUAC | `backends/contracts/uniqac.py` | 活度系数 | 📝 **CONTRACT** |
| Wilson | `backends/contracts/wilson.py` | 活度系数 | 📝 **CONTRACT** |
| SRK | `backends/contracts/srk.py` | 状态方程 | 📝 **CONTRACT** |

> **注**：CONTRACT 状态表示该模型当前只有类型化接口和模型卡片，没有生产求解器。如需使用，请等 Phase 4 上线或联系责任人。

---

## 1. Ideal / Raoult

### 正式名称
**Ideal Gas / Raoult's Law**（理想气体/拉乌尔定律）

### 模型类型
理想定律（低压基线模型）

### 支持任务
泡点温度、泡点压力、露点温度、露点压力、TP 闪蒸、等压 Txy、等温 Pxy

### 输入参数
| 参数 | 格式 | 必须 | 说明 |
|---|---|---|---|
| `components` | `list[str]` | 是 | 组分名称 |
| `temperature` | `float` | 否 | 温度，K |
| `pressure` | `float` | 否 | 压力，Pa |
| `composition` | `list[float]` | 是（泡/露点） | 摩尔分数 |
| `temperature_range` | `[float, float]` | 否 | Txy 扫描范围 |
| `pressure_range` | `[float, float]` | 否 | Pxy 扫描范围 |

### 适用条件
| 条件 | 范围 |
|---|---|
| 压力 | 低压（< 0.2 MPa） |
| 体系 | 非极性/弱非理想体系 |
| 温度 | 由 Antoine 常数温度范围决定 |
| 组分数量 | 最多 2 个 |

### 局限性
- 仅适用于低压，高压下误差急剧增大
- 不能处理极性体系（醇-水等含氢键体系）
- 不能预测共沸现象
- 内置 Antoine 常数仅覆盖 10 种常见组分
- 假设理想气相，不计算逸度系数

### 可执行状态
| 项目 | 状态 |
|---|---|
| `calculate()` 实现 | ✅ 已完成，含泡点/露点/Txy 曲线计算 |
| 单元测试 | ✅ 通过 |
| Antoine 常数源 | NIST Chemistry WebBook（mmHg 版本） |

---

## 2. Peng-Robinson

### 正式名称
**Peng-Robinson Equation of State**（PR 立方型状态方程）

### 模型类型
立方型状态方程（EOS）

### 支持任务
泡点、露点、TP 闪蒸、等压 Txy（二元）、相态分类

### 输入参数
| 参数 | 格式 | 必须 | 说明 |
|---|---|---|---|
| `components` | `list[str]` | 是 | 组分名称 |
| `temperature` | `float` | 是 | 温度，K |
| `pressure` | `float` | 是 | 压力，Pa |
| `composition` | `list[float]` | 否 | 进料组成 |
| `parameters.kijs` | `ndarray(N,N)` | 否 | 二元交互参数 |
| `parameters.Tcs` | `ndarray(N,)` | 否 | 临界温度（可自动查找） |
| `parameters.Pcs` | `ndarray(N,)` | 否 | 临界压力（可自动查找） |
| `parameters.omegas` | `ndarray(N,)` | 否 | 偏心因子（可自动查找） |

### 适用条件
| 条件 | 范围 |
|---|---|
| 压力 | 中高压（0.1 ~ 数十 MPa） |
| 体系 | 非极性/弱极性（烃类、天然气、炼油） |
| 温度 | 低于临界温度 |
| 典型 kij | 同类烃 0~0.01，CO2-烃 0.05~0.15 |

### 局限性
- 极性体系（醇、水、酸）精度差，需高级混合规则
- 液相体积预测系统性偏大（约 5~10%）
- 临界区 (Tr≈0.95~1.05) 精度显著下降
- kij 假设为常数但实际随温度变化
- 当前 Phase 4 前只实现部分任务（bubble_T, isobaric_Txy）

### 内置参数库
内置 17 种常见组分的 Tc, Pc, omega（甲烷、乙烷、苯、甲苯、CO2 等）。

---

## 3. NRTL

### 正式名称
**Non-Random Two-Liquid**（非随机双液模型）

### 状态
📝 **CONTRACT** —— 仅类型化接口 + 模型卡，求解器 Phase 4 实现

### 支持任务
泡点、泡点压力、等压 Txy

### 所需参数（Phase 4 时提供）
| 参数 | 格式 | 必须 | 说明 |
|---|---|---|---|
| `tau_coeffs` | `ndarray(N,N,6)` | 是 | 二元交互参数 [a,b,e,f,g,h] |
| `alpha_coeffs` | `ndarray(N,N,2)` | 否 | 非随机性参数，默认 0.3 |

### 适用条件
- 压力：中低压（< 2 MPa）
- 体系：极性、含氢键、部分互溶体系
- 组分：最多 5 个

### 局限性
- 需结合 EOS 处理气相非理想性
- 参数质量决定精度
- 压力不敏感，不可单独用于高压

---

## 4. UNIQUAC

### 正式名称
**UNIversal QUAsi-Chemical**（通用准化学模型）

### 状态
📝 **CONTRACT** —— 仅类型化接口 + 模型卡，求解器 Phase 4 实现

### 支持任务
泡点、泡点压力、等压 Txy

### 所需参数（Phase 4 时提供）
| 参数 | 格式 | 必须 | 说明 |
|---|---|---|---|
| `tau_coeffs` | `ndarray(N,N,6)` | 是 | 二元交互参数 |
| `rs` | `ndarray(N,)` | 否 | 体积参数（可自动查找） |
| `qs` | `ndarray(N,)` | 否 | 表面积参数（可自动查找） |

### 适用条件
- 适用范围比 NRTL 广（含聚合物体系）
- 理论基础强于 NRTL，参数温度依赖性更合理

### 局限性
- 与 NRTL 同等局限性
- 部分互溶体系 NRTL 收敛更稳定

---

## 5. Wilson

### 正式名称
**Wilson Equation**（Wilson 方程）

### 状态
📝 **CONTRACT** —— 仅类型化接口 + 模型卡，求解器 Phase 4 实现

### 支持任务
泡点、泡点压力、等压 Txy

### 所需参数（Phase 4 时提供）
| 参数 | 格式 | 必须 | 说明 |
|---|---|---|---|
| `Lambda_coeffs` | `ndarray(N,N,6)` | 是 | Wilson 交互参数 |
| `volumes` | `ndarray(N,)` | 是 | 液体摩尔体积 |

### 适用条件
- 完全互溶体系 VLE 首选

### 局限性
- **不能处理 LLE**（部分互溶体系 Wilson 方程失效）
- 路由规则应排除 LLE 任务

---

## 6. SRK

### 正式名称
**Soave-Redlich-Kwong Equation of State**（Soave 改进的 Redlich-Kwong 状态方程）

### 状态
📝 **CONTRACT** —— 仅类型化接口 + 模型卡

### 支持任务
泡点、泡点压力

### 所需参数（需要时可从 PR 适配器改造）
| 参数 | 格式 | 必须 | 说明 |
|---|---|---|---|
| `kijs` | `ndarray(N,N)` | 否 | 二元交互参数 |

### 说明
SRK 与 PR 功能重叠度高，当前主项目优先部署 PR。如需 SRK 的可运行版本，可直接将 `peng_robinson.py` 中的 `PRMIX` 替换为 `SRKMIX`。

---

## 模型选择快速参考（给 LLM 决策层的参考）

```
用户的问题涉及什么体系？
│
├─ 低压、非极性、简单体系 → Ideal / Raoult [RUNNABLE]
│   （如苯-甲苯常压精馏）
│
├─ 中高压、烃类、天然气 → Peng-Robinson [RUNNABLE]
│   （如 CO2-甲烷高压分离）
│
├─ 极性、醇-水、含氢键 → 活度系数模型 [CONTRACT]
│   ├─ 部分互溶 → NRTL
│   ├─ 含聚合物 → UNIQUAC
│   └─ 完全互溶 → Wilson
│   （注：求解器 Phase 4 上线，当前无法执行）
│
└─ 功能被 PR 覆盖 → SRK（暂不部署，同 PR 团队协调）
```

---

## 附录：与旧版 `thermo_calculator` 的差异

| 项目 | 旧版 (thermo_calculator) | 新版 (thermo_engine) |
|---|---|---|
| 核心接口 | `model.predict(...)` | `adapter.calculate(manifest: TaskManifest)` |
| 输入格式 | 松散参数列表 | 类型化 `TaskManifest` |
| 模型管理 | 直接实例化 | `BackendRegistry` 统一管理 |
| Ideal/Raoult | 未实现 | ✅ 新增可运行 |
| NRTL/UNIQUAC | 完整 `predict()` 代码 | 📝 降级为 CONTRACT 合同层 |
| SRK | 完整 `predict()` 代码 | 📝 降级为 CONTRACT 合同层 |
| 模型卡 | 无 | ✅ 每个模型有 `ModelCard` |
| AI 预留 | 无 | ✅ `backends/ai/` 目录预留 |
